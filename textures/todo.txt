to demonstrate: 

noise - induced ordering: 
1. A -> B -> trivial 
2. A -> B -> A -> trivial 

with injecting seeds every 16 iterations, does much better than with every 8. 

leopard model: take out hidden layers 5, 6, 9, and it does synchronized flashing! 